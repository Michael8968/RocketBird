下面我结合你提供的附件内容（会员端整体方案）+ 你当前的技术需求，给出**项目一期可落地的产品规划、技术架构、以及开发栈选择**，确保：

* **后端可部署在腾讯云 TCB（腾讯云开发）**
* **前端包括：H5（会员端） + 管理后台（运营后台）**
* **支持后续平滑扩展到微信小程序**
* **技术栈提前为微信小程序留好兼容性与复用能力**

---

# ✅ 一、一期项目整体需求（根据附件内容精简而可落地）

附件里的功能很多，但一期不建议全部上，可以先做：

## **📱 H5 会员端（一期上线版）**

主要功能：

* 会员登录注册（手机号 + 微信授权）
* 会员等级展示
* 会员权益展示
* 积分体系（积分展示、积分明细）
* 积分兑换（简单的虚拟兑换）
* 打卡分享（图文上传 + 分享海报）
* 每日健身餐展示（图文）
* 推荐好友（生成海报 + 链接）
* 我的顾问（私教顾问展示，仅展示功能）
* 意见反馈

## **🖥️ 管理后台（一期上线版）**

* 登录 / 权限（简单版）
* 会员管理
* 会员积分管理
* 会员等级与权益配置
* 积分商品管理
* 打卡内容审核/管理
* 健身餐内容管理
* 意见反馈管理
* 数据统计（积分、用户数量基础统计）

> 一期可以先把“运营模块 + 基础会员能力”上线
> 二期再上赛事、秒杀、大转盘、小程序联动。

---

# ✅ 二、技术方案（重点：兼容微信小程序、可上 TCB）

## **后端：腾讯云 TCB（Node.js 云函数）**

选型原因：

* 原生支持 Node.js
* 提供云数据库（MongoDB）
* 提供云存储、云托管
* 后期微信小程序可无缝对接（使用同一个 TCB 环境）

后端结构：

* API 使用 **TCB 云函数（HTTP 形式）**
* 数据库：TCB MongoDB
* 文件（打卡图片/短视频/健身餐图）：TCB 云存储
* 认证：微信登录 + 手机验证码登录（用云开发短信）

## **前端技术栈（重点为未来小程序做铺垫）**

### **会员端 H5：使用 UniApp（Vue3 + Vite）**

为什么？

* UniApp 一套代码，可输出 H5 + 微信小程序
* 一期做 H5，二期直接编译成小程序，复用 80% 代码
* 支持微信授权、文件上传、分享海报等功能

构建方式：

* UniApp + Vue3 + Pinia + Vite
* UI 可用 uView 2 或 uni-ui

### **管理后台：React + Vite + Ant Design**

管理后台对小程序无复用需求，直接用最佳组合即可。

你之前的偏好（Dora → Framer → Wegic）偏向前端可视化开发，但后台不适合这些工具，因此：

管理后台推荐：

* React 18
* Vite
* Ant Design 5
* SWR / React Query

### **接口统一规范**

推荐统一 RESTful 或者简单 GraphQL（但 TCB 对 REST 更友好）

---

# ✅ 三、项目架构图（适合 TCBD + 多端方案）

```
                               ┌───────────────────────────┐
                               │      管理后台 React       │
                               │  (Vite + AntD + SWR)      │
                               └───────────┬───────────────┘
                                           │ HTTPS API
┌──────────────────────────┐               │
│     H5 会员端（UniApp）   │──────────────▶│
│  Vue3 + Pinia + Vite      │               ▼
└──────────────────────────┘       ┌─────────────────────────┐
                                   │    腾讯云 TCB 云函数     │
┌──────────────────────────┐       │  Node.js + REST API     │
│ 微信小程序（后期版本）    │──────▶│  认证 / 会员 / 积分等   │
│（UniApp 直接编译）        │       └──────────┬──────────────┘
└──────────────────────────┘                  │
                                              │
                                      ┌───────▼────────┐
                                      │ TCB 云数据库    │
                                      │ MongoDB         │
                                      └──────────────────┘
                                      ┌──────────────────┐
                                      │ 云存储（图片视频）│
                                      └──────────────────┘
```

---

# ✅ 四、数据库设计（一期核心表）

### **1. user（会员）**

```
userId
openid
phone
nickname
avatar
level
points
inviteCode
invitedBy
createdAt
```

### **2. points_log（积分流水）**

```
userId
type: income | cost
value
source: sign | share | exchange | admin
createdAt
```

### **3. goods（积分商品）**

```
goodsId
title
type: virtual / physical
points
stock
image
status
createdAt
```

### **4. checkin（打卡）**

```
checkinId
userId
images: []
content
status
createdAt
```

### **5. meals（每日健身餐）**

```
mealId
date
title
content
images
```

---

# ✅ 五、一期开发排期（4–6 周）

### **第 1 周：技术准备 + 后端基础**

* TCB 云环境创建
* 用户登录/注册/权限
* 数据库 schema 建立
* 管理后台模板搭建
* H5 项目初始化（UniApp）

### **第 2 周：会员体系 / 积分体系**

* 会员信息管理接口
* 积分规则
* 积分商城（前后端）
* 管理后台积分商品管理

### **第 3 周：打卡分享 / 健身餐**

* 打卡上传（含图片上传）
* 健身餐管理（后台）
* 打卡列表、个人页

### **第 4 周：推荐好友 / 意见反馈**

* 邀请海报
* 分享逻辑
* 建议反馈表单 & 后台审核

### **第 5 周：管理后台完善**

* 会员管理
* 积分统计
* 权限体系
* 内容审核模块

### **第 6 周：测试与上线**

* 压测
* H5 发布到 TCB 静态托管
* 后端云函数上线
* 上线验收

---

# ✅ 六、你如果需要，我可以继续输出：

### ✔ UI 原型（H5 + 后台）

### ✔ UniApp 项目脚手架（可直接拉 Git）

### ✔ TCB 云函数模板

### ✔ 完整接口文档（OpenAPI）

### ✔ 二期小程序架构

---

# ✅ 最后：如需我帮你生成 **“完整项目文件结构 + 代码初始化”**

只要告诉我：

👉 **要不要我帮你生成 UniApp + TCB + React 管理后台的项目模板？**
